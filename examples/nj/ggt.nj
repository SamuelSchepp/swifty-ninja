//
// compute the gcd of two positive numbers
//

void main() {
  local Integer x;
  local Integer y;
  x = 60;
  y = 1120;
  while (x != y) {
    if (x > y) {
      x = x - y;
    } else {
      y = y - x;
    }
  }
  writeInteger(x);
  writeCharacter('\n');
}
